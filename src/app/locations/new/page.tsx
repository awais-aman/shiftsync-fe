import type { NextPage } from "next";
import { NewLocation } from "@/app/locations/new/_components";

const NewLocationPage: NextPage = () => {
  return <NewLocation />;
};

export default NewLocationPage;
