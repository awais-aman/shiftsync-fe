export enum UserRole {
  Admin = "admin",
  Manager = "manager",
  Staff = "staff",
}

export enum QueryKeys {
  CurrentUser = "currentUser",
  Locations = "locations",
  Location = "location",
  Skills = "skills",
  Skill = "skill",
}

export enum AuthMode {
  Login = "login",
  Signup = "signup",
}
