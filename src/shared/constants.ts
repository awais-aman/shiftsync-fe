export enum UserRole {
  Admin = "admin",
  Manager = "manager",
  Staff = "staff",
}

export enum QueryKeys {
  CurrentUser = "currentUser",
}

export enum AuthMode {
  Login = "login",
  Signup = "signup",
}
